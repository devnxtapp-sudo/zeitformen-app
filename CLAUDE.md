# Tense Trainer

**Repo:** `devnxtapp-sudo/zeitformen-app` (⚠️ Ordner heißt `tense-trainer`, Repo heißt `zeitformen-app`)
**App:** Tense Trainer — „16 Zeitformen aus 8 Bausteinen". Rein **statisch** (`index.html` / `app.js` / `style.css`). **Kein Backend, kein Login, kein Build-Schritt.**

## Deploy
- **GitHub Pages**, Branch `main` / root, Repo ist **public**, `.nojekyll` vorhanden.
- Live: **https://devnxtapp-sudo.github.io/zeitformen-app/**
- Veröffentlichen: `git push origin main` → Pages baut automatisch.
- Asset-Pfade müssen **relativ** bleiben (`style.css`, `app.js`), da die Seite unter dem Unterpfad `/zeitformen-app/` läuft.

## Wichtig
- **Nur an diesem Repo arbeiten.** Schwester-Repos **NICHT anfassen**: `training-app` (rxZone), `justfleet`, `justfleet-backend`.
- Antworten auf Deutsch.
